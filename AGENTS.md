## Learned Rules

### Conventions
- Never put examples, paths, or copy from other user repositories into this package.

### Tooling
- After editing this package, refresh the installed copy under the harness skills path (Cursor: ~/.cursor/skills/learn-skill). A symlink stays in sync; a copy must be updated after each change.
